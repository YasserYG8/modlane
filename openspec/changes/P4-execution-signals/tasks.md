# P4 — Tasks

- [x] Extend `ChatRequest` in `src/providers/types.ts` to retain `dialect` and `rawBody`.
- [x] Update request parser in `src/server.ts` to populate `dialect` and `rawBody`.
- [x] Implement signal extraction and heuristics in `src/signals.ts`.
- [x] Cover extraction rules for OpenAI format with unit tests in `src/signals.test.ts`.
- [x] Cover extraction rules for Anthropic format with unit tests in `src/signals.test.ts`.
- [x] Verify build and vitest run successfully.
